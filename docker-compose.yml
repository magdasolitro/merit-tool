# Local smoke-test of the x-reg image (same tag as terraform/setup.sh → VM).
# Set XREG_PROJECT_ROOT if your clone is not at /home/msolitro/x-reg.
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: xreg:latest
    pull_policy: missing
    container_name: xreg-local
    ports:
      - "3000:3000"
